{
  "name": "zeta-hackathon",
  "version": "1.0.0",
  "description": "ZetaChain Cross-Chain Lending Protocol",
  "private": true,
  "workspaces": [
    "lending-zeta",
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --filter frontend dev",
    "build": "bun run --filter frontend build",
    "test": "bun run --filter lending-zeta test",
    "deploy": "bun run --filter lending-zeta deploy"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "bun@1.0.0",
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.3",
    "@radix-ui/react-switch": "^1.2.5",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.525.0",
    "tailwind-merge": "^3.3.1",
    "tailwindcss-animate": "^1.0.7"
  }
}